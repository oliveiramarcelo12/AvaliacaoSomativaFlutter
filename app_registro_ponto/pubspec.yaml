name: app_registro_ponto
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Dependências do Firebase
  firebase_core: ^3.6.0            # Versão mais recente
  firebase_auth: ^5.3.1           # Para autenticação
  cloud_firestore: ^5.4.4         # Versão mais recente
  geolocator: ^13.0.1              # Versão mais recente

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
