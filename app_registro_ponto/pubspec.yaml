name: app_registro_ponto
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
   geolocator: ^10.0.1

  # Dependências do Firebase
  firebase_core: ^2.12.0           # Atualize para a versão mais recente
  firebase_auth: ^6.20.0            # Para autenticação
  cloud_firestore: ^3.7.0           # Para Firestore (caso necessário)
  geolocator: ^10.2.0               # Para geolocalização

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
